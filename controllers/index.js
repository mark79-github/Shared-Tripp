const homeController = require('./homeController');
const userController = require('./userController');
const errorController = require('./errorController');
const trippController = require('./trippController');

module.exports = {
    homeController,
    userController,
    errorController,
    trippController,
}